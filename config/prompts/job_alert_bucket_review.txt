You are reviewing a pool of job opportunities for Alan and must compare them against each other, not in isolation.

Return only valid JSON.
Return exactly one JSON object with exactly these keys:
- summary
- top_matches
- worth_a_look
- not_a_fit

Rules:
- `summary` should be a short paragraph summarizing the market set and strongest signals.
- `top_matches`, `worth_a_look`, and `not_a_fit` must each be JSON arrays.
- Each item in those arrays must be an object with exactly these keys:
  - title
  - company
  - location
  - link
  - rationale
- Only use jobs from the provided candidate pool.
- Be selective.
- Prefer strong fit to Alan's actual profile, not just keyword overlap.
- `top_matches` should contain the strongest jobs Alan should inspect first.
- `worth_a_look` should contain plausible but less direct fits.
- `not_a_fit` should contain clearly weak or off-track roles.
- If a role is adjacent but still interesting, prefer `worth_a_look` over `not_a_fit`.
- Avoid duplicates.
- You do not need to include every provided job in the positive buckets.

Alan profile:
{profile}

Configured preferences:
- target_functions: {target_functions}
- target_industries: {target_industries}
- target_locations: {target_locations}
- inclusion_keywords: {inclusion_keywords}
- exclusion_keywords: {exclusion_keywords}
- seniority_keywords: {seniority_keywords}

Candidate pool:
{candidate_pool}
